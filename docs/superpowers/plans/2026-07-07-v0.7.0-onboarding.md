# v0.7.0 Onboarding Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the v0.7.0 first-run readiness summary and friendly sync status labels.

**Architecture:** Keep logic in a pure `onboardingStatus` service and reuse it from `AppShell` and `LocalHistory`. Avoid real external checks; surface state already available through existing Electron bridge clients.

**Tech Stack:** React, TypeScript, Electron preload APIs, Node test runner with `tsx`.

## Global Constraints

- Do not open or print `.env`.
- Hardware, camera, installer execution, and real Supabase checks stay as user checklist items.
- Firmware pin remains GPIO3, baud rate remains 9600, ACKs remain `ACK:BAD:80` and `ACK:GOOD:10`.
- v0.7.0 scope only: do not implement multi-group, installer, tray, updater, or v1.0 release work.

---

### Task 1: Status Helpers

**Files:**
- Create: `turtleguard/src/services/onboardingStatus.ts`
- Create: `turtleguard/test/onboardingStatus.test.mjs`

- [ ] Write tests for hardware, camera, social, overall readiness, and sync status copy.
- [ ] Run the test and confirm it fails because the service does not exist.
- [ ] Implement the pure helper functions.
- [ ] Run the test and confirm it passes.

### Task 2: App Onboarding UI

**Files:**
- Modify: `turtleguard/src/components/AppShell.tsx`

- [ ] Load hardware and settings status with existing clients.
- [ ] Render one readiness card above the active tab.
- [ ] Add CTA buttons that switch to the relevant tab.
- [ ] Keep camera check manual and mark it ready after the user visits the monitor tab.

### Task 3: Friendly Sync Labels

**Files:**
- Modify: `turtleguard/src/pages/LocalHistory.tsx`

- [ ] Replace raw sync status text with `getFriendlySyncStatus`.
- [ ] Keep retry behavior limited to `pending_sync`.

### Task 4: Verification and Checkpoint

- [ ] Run `npm.cmd run test:node`.
- [ ] Run `npm.cmd run test:mvp2`.
- [ ] Run `npm.cmd run lint`.
- [ ] Run `npm.cmd run build`.
- [ ] Commit the v0.7.0 checkpoint after all commands pass.
