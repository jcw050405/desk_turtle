# v0.6.0 Release Readiness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prepare TurtleGuard for an initial public handoff with clear install, firmware, wiring, Supabase, and next-chat documentation.

**Architecture:** Keep runtime code stable and focus on packaging metadata plus markdown docs. Avoid touching secrets or requiring real hardware/Supabase checks in automation.

**Tech Stack:** Electron Builder, Vite, React, ESP32-C3 Arduino firmware, Supabase SQL migration.

## Global Constraints

- Do not open or print `.env`.
- Hardware, camera, installer execution, and real Supabase checks stay as user checklist items.
- Firmware pin remains GPIO3, baud rate remains 9600, ACKs remain `ACK:BAD:80` and `ACK:GOOD:10`.

---

### Task 1: Package Metadata

**Files:**
- Modify: `turtleguard/package.json`
- Modify: `turtleguard/package-lock.json`
- Modify: `turtleguard/metadata.json`

- [ ] Bump version to `0.6.0`.
- [ ] Add Electron Builder app id and Windows installer artifact name.
- [ ] Align metadata description with the Electron desktop app.

### Task 2: Public Setup Docs

**Files:**
- Modify: `turtleguard/README.md`
- Modify: `turtleguard/.env.example`

- [ ] Replace AI Studio template README with TurtleGuard install/run instructions.
- [ ] Document ESP32-C3 firmware upload and MG90S wiring.
- [ ] Document Supabase migration and environment variables.
- [ ] Add manual verification checklist for user-only checks.

### Task 3: Next Handoff

**Files:**
- Create: `docs/handoff-chat-2-v0.7.0-to-v1.0.0-prompt.md`

- [ ] Summarize v0.3.0 through v0.6.0 completion.
- [ ] Define suggested v0.7.0 through v1.0.0 goals.
- [ ] Include exact project paths and hardware facts.

### Task 4: Verification

- [ ] Run `npm.cmd run test:node`.
- [ ] Run `npm.cmd run test:mvp2`.
- [ ] Run `npm.cmd run lint`.
- [ ] Run `npm.cmd run build` to verify renderer production build.
